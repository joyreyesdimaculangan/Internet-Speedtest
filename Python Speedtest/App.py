import tkinter as tk
from AppContent import AppContent

# Driver Code
if __name__ == '__main__': 

    root = tk.Tk() # initializing the main window
    open =  AppContent(root)
    root.mainloop() # continues to loop the tkinter application